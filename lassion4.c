// Programe to latterband its number
#include <stdio.h>
void main()
{

    char alpha;
    int num
    printf("Enter Alphabet  ");
    scanf("%c", &alpha);
    if (alpha == 'a' || 'A')
    {
        num = 1;
        printf("This alphabet no is  %d", num);
    }
    if (alpha == 'b' || 'B')
    {
        num = 2;
        printf("This alphabet no is  %d", num);
    }
    if (alpha == 'c' || 'C')
    {
        num = 3;
        printf("This alphabet no is  %d", num);
    }
    if (alpha == 'd' || 'D')
    {
        num = 4;
        printf("This alphabet no is  %d", num);
    }
    if (alpha == 'e' || 'E')
    {
        num = 5;
        printf("This alphabet no is  %d", num);
    }
    
}